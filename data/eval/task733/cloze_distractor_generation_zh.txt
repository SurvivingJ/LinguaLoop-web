句子：{original_sentence}
填空：{sentence_with_blank}
正确答案：{correct_answer}
学习者级别：{complexity_tier}

生成恰好 3 个干扰项，它们都是该句填空处的错误选项。

硬性规则：
1. 每个干扰项的词性必须与正确答案相同。
2. 每个干扰项填入空格后必须在语法上成立。
3. 每个干扰项在这个具体句子的语境中必须是错误的。
4. 干扰项不得是正确答案的同音词、近音词、变体形式，也不得是正确答案的组成部分。
5. 干扰项不得与正确答案"{correct_answer}"相同，三个干扰项之间也不得重复。

输出每个干扰项之前，必须完成以下自检：
(a) 失误维度 —— 为每个干扰项从下列各项中恰好指定一个失误维度：
    - "semantic"      ：指称类别错误 / 概念错误
    - "collocational" ：与周围词语搭配不自然
    - "aspectual"     ：词汇体 / 事件结构错误
    - "register"      ：语体、领域或社交场合不符
    - "valency"       ：论元结构错误 / 补语错误
(b) 替换审查 —— 设想正确答案的一个常见近义词，并将其替换进句子。如果替换为该近义词后，你的干扰项会变成可接受的选项，就否决这个干扰项，另选一个。干扰项必须在近义词替换之后，在该句子中仍然是错误的。

三个干扰项之中，至少要出现两种不同的失误维度 —— 不允许出现全部雷同或“错得很像”的一组干扰项。
最后把每个干扰项放回空格重读一遍。如果它在该语境中读起来正确或可接受，就在输出前替换掉它。

返回 JSON：
{{"distractors": ["word1","word2","word3"], "distractor_tags": {{"word1":"semantic","word2":"collocational","word3":"valency"}}, "explanation": "正确答案为何正确的简要说明。"}}

如果另外生成选项列表，把正确答案放在第一位 —— 不要打乱顺序。
